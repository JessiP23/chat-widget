map $http_origin $allow_origin {
    default                             "";
    "~^https://(www\.)?wmstudio\.io$"  $http_origin;
    "~^http://localhost(:[0-9]+)?$"    $http_origin;
    "~^http://127\.0\.0\.1(:[0-9]+)?$" $http_origin;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index embed.html;

    add_header Content-Security-Policy  "frame-ancestors https://wmstudio.io https://www.wmstudio.io http://localhost:* http://127.0.0.1:*" always;
    add_header Access-Control-Allow-Origin  $allow_origin always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type, Authorization" always;

    if ($request_method = OPTIONS) {
        return 204;
    }

    # ── Proxy WebSocket to backend (must be before /api/v1/) ─────────────
    location /api/v1/ws/ {
        proxy_pass         https://wm-chatbot-api.fly.dev;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection $connection_upgrade;
        proxy_set_header   Host       wm-chatbot-api.fly.dev;
        proxy_set_header   X-Real-IP  $remote_addr;
        proxy_ssl_server_name on;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # ── Proxy REST API to backend ─────────────────────────────────────────
    location /api/v1/ {
        proxy_pass         https://wm-chatbot-api.fly.dev;
        proxy_http_version 1.1;
        proxy_set_header   Host              wm-chatbot-api.fly.dev;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
    }

    # ── Static assets ─────────────────────────────────────────────────────
    location ~* \.(js|html)$ {
        add_header Cache-Control           "public, max-age=60";
        add_header Access-Control-Allow-Origin $allow_origin always;
        add_header Content-Security-Policy "frame-ancestors https://wmstudio.io https://www.wmstudio.io http://localhost:* http://127.0.0.1:*" always;
        try_files $uri =404;
    }

    # ── Health check ──────────────────────────────────────────────────────
    location /health {
        return 200 "ok";
        add_header Content-Type text/plain;
    }
}
